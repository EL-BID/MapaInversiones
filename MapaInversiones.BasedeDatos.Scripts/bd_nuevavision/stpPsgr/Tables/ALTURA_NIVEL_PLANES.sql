CREATE TABLE [stpPsgr].[ALTURA_NIVEL_PLANES] (
    [idAlturaNivelPlanes] INT           NOT NULL,
    [nombre]              VARCHAR (100) NULL,
    CONSTRAINT [PK_ALTURA_NIVEL_PLANES] PRIMARY KEY CLUSTERED ([idAlturaNivelPlanes] ASC)
);

